import { CarditImgCompressorDirective } from './cardit-img-compressor.directive';

describe('CarditImgCompressorDirective', () => {
  it('should create an instance', () => {
    const directive = new CarditImgCompressorDirective();
    expect(directive).toBeTruthy();
  });
});
